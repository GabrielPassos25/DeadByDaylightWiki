import React, { useState } from 'react';
import {Image, View,Text, TouchableOpacity, StyleSheet} from 'react-native';
const Home = () =>{
    return(
        <View style ={{flex:1, flexDirection:'row',padding:10, justifyContent:'center', marginTop:10}}></View>
    )
}

export default Home;